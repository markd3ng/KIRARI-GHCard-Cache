## 完整重写项目技术文档

按照 tech-doc-writer 规范（结论先行、高信息密度、无废话、统一命名空间、禁止 AI 句式）重写了全部 `.md` 文档：

### 变更文件
- **README.md** — 175 行，精简重构。去掉了重复的 FAQ/开发流程，将 API、配置、缓存策略、快速开始整合为紧凑表格 + 代码块
- **docs/DEPLOYMENT.md** — 60 行，部署入口。平台选择 + 变量归属总表 + CI 权限
- **docs/CLOUDFLARE_DEPLOYMENT.md** — 163 行，8 步完整部署流程
- **docs/VERCEL_DEPLOYMENT.md** — 126 行，独立项目 + 同项目 adapter 双模式
- **docs/KIRARI_INTEGRATION.md** — 156 行，三种对接模式 + token 归属表
- **docs/OPERATIONS.md** — 134 行，troubleshooting-first 运维指南
- **docs/ARCHITECTURE.md** — 227 行（新增），模块依赖图、双平台数据流、缓存层对比、错误处理策略

### 文档规范
- 所有代码块标记语言（`typescript` / `bash` / `toml` / `jsonc` / `text`）
- 参数/对比全部使用 Markdown 表格
- 禁止使用"首先其次最后"、"我们可以看到"、"这使得"等 AI 句式
- 高亮块（`>`）仅用于核心警告和前置条件
- 每个 section 只讲一个概念
- API 参数包含边界条件（size 范围、status code 策略）

## 修复 DOC_REVIEW_REPORT.md 审计发现的 5 个问题

对照 `docs/DOC_REVIEW_REPORT.md`（外部 AI 生成的审计报告）验证并修复了全部问题：

1. **[高优] healthz 响应字段不一致** — Cloudflare 实际返回 `service` 而非 `runtime`。修复 README API 表，所有文档增加跨平台对比说明
2. **[中优] Prewarm URL 构造未文档化** — 新增 prewarm URL fallback 说明（`/prewarm-placeholder` 路径 + `prewarm.local` fallback）
3. **[低优] X-KIRARI-GHC-PUBLIC-BASE header 缺乏规范** — 补充三级解析优先级和 Vercel 差异
4. **[低优] Body 大小限制未文档化** — 新增 MAX_JSON_BYTES (1 MB) / MAX_AVATAR_BYTES (512 KB) 说明
5. **[低优] GITHUB_TOKEN 对 avatar 不生效** — 明确 token 仅用于 REST API，avatar 走 CDN 匿名访问

commit: `833f1a8` — `docs: repair documentation discrepancies found in audit report` — 已 push
